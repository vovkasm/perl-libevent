* allow $event->add without arguments
* rename $io_event->io -> $io_event->fh ?
* Correctly check arguments
* Remove pointer to perl-level event_base from event structs (use global hash-table instead?)
* Optimizations... testing...
* Release to CPAN
* ? Threads...
* Add bufferevents
* Add DNS
* Add HTTP
* ? RPC
