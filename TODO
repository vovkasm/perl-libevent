* Release to CPAN
* Remove pointer to perl-level event_base from event structs (use global hash-table instead?)
* Optimizations... testing...
* ? Threads...
* Add bufferevents
* Add DNS
* Add HTTP
* ? RPC
